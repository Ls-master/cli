export default {
  /*'dep.isShow'(newVal) {
    const root = this.$root

    if (!newVal) {
      return
    }

    root.getDepList(() => {
      root.getAllUser((list, map) => {
        list.forEach((item) => {
          item.checked = root.user.mapCheckedDefault[item.id]
        })
      })
    })
  },*/
  /*'$route.query.pathBreadList'() {
    const root = this.$root
    root.pathBreadOperate()
  },*/
  /*'$route.path'(newVal) {
    const root = this.$root
    root.pathBreadOperate()
  }*/
}